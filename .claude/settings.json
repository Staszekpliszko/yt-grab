{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "shell": "bash",
            "command": "cd \"D:/PROGRAMOWANIE/YoutubeDownload\" && git add -A && { git diff --cached --quiet || { git commit -m \"chore: auto-commit po sesji [skip ci]\" && git push; }; }",
            "timeout": 120,
            "statusMessage": "Auto-commit + push po sesji…"
          }
        ]
      }
    ]
  }
}
